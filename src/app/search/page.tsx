import SearchBar from "../components/searchbar";

const Search = () => {
  return (
    <main className="flex flex-col items-center justify-start min-h-screen w-full">
      <div className="mt-10"> {/* Adjust margin as needed */}
        <SearchBar />
      </div>
          <section>
              
      </section>
    </main>
  );
};

export default Search;
